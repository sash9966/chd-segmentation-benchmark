# paths_server.sh — machine-specific paths, sourced by scripts/server_run.sh
# Copy to configs/paths_server.sh — this file is gitignored, never committed.

# ── nnUNet paths ───────────────────────────────────────────────────────────────
export nnUNet_raw="/scratch/users/sastocke/nnunet_CHD/nnUNet_raw"
export nnUNet_preprocessed="/scratch/users/sastocke/nnunet_CHD/nnUNet_preprocessed"
export nnUNet_results="/scratch/users/sastocke/nnunet_CHD/nnUNet_results"

# ── Dataset ────────────────────────────────────────────────────────────────────
export DATASET_NAME="Dataset030_imageCHD_HU"
export DATASET_ID=30

# ── Derived dataset paths ──────────────────────────────────────────────────────
export NNUNET_RAW_DATASET="${nnUNet_raw}/${DATASET_NAME}"
export NNUNET_PREPROCESSED="${nnUNet_preprocessed}/${DATASET_NAME}"

# ── Conda ──────────────────────────────────────────────────────────────────────
export CONDA_BASE="/oak/stanford/groups/amarsden/sastocke/miniconda"

# ── Scratch / repo ─────────────────────────────────────────────────────────────
export SCRATCH="/scratch/users/sastocke"
export REPO="${SCRATCH}/chd-segmentation-benchmark"

# ── Conda envs ─────────────────────────────────────────────────────────────────
export TOTALSEG_ENV="${SCRATCH}/miniconda/envs/totalseg_env"
export MEDSAM2_ENV="${SCRATCH}/miniconda/envs/medsam2_env"
